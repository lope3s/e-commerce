user service:
    [x] - enable auto reload after code changes;
    [x] - register user
        [x] - create a hash password function;
        [x] - check why knex config isn't working with sqlite3 but is working with pg;
        [x] - check why jest .toThrow() method isn't catching the errors properly;
        [x] - create a user service;
        [] - check if there's a better way to close the connection to the database, so jest stop complaining about forcing proccesses to exit;
    [x] - login user
